
ips = []

with open ('valid_proxies.txt') as f:
    proxies = f.read().splitlines()
    proxies = ips.append(proxies)
    #print(ips)

# It reads the file valid_proxies.txt and stores the proxies in a list.
